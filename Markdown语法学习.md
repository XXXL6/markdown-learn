# Markdown语法学习



- Typora直接编写文本 --> 自动转换为Markdown语句 --> 自动渲染出文档

- Typora快捷键 --> Markdown语句对应的形式、标记 --> 渲染出的文档对应的效果



## 一. 标题语法

几个`#`表示几级标题（1~6）

Typora快捷键：ctrl + x（1~6，x是几就是几级标题）

```
# 一级标题
## 二级标题
### 三级标题
```

渲染后效果：

# 一级标题
## 二级标题
### 三级标题



## 二. 段落语法

使用空白行表示将一行或多行文本进行分隔，

尽量不要用空格（spaces）或制表符（ tabs）缩进段落。

（当然，Typora中一行写完后按下回车即可自动分段，生成的Markdown语句中会自动空出一行）



## 三. 换行语法

在一行末尾添加两个或多个空格，然后按回车键，即可在Markdown语句中创建一个换行标识符`<br>`，渲染时遇到`<br>` 就会将文字换到下一行

> [!NOTE]
>
> 注意： Markdown中换行 ≠ 创建新段落
>
> ​	    创建新段落 = Markdown语句中空白一行 = Typora中回车
>
> ​	    换行 = Markdown语句中换行标识符`<br>` = Markdown语句中两个空格然后按回车键（创建`<br>`） = Typora 中写`<br>`
>
> ​	    Markdown中读取到回车“`\r\n`”，会直接保留回车换行，而不进行任何渲染



```
我喜欢学习<br>我喜欢记笔记学习<br>我太喜欢学习了
```

或

```
我喜欢学习  
我喜欢记笔记学习  
我太喜欢学习了  
```

渲染后效果：

我喜欢学习<br>我喜欢记笔记学习<br>我太喜欢学习了



```
我喜欢学习

我喜欢记笔记学习

我太喜欢学习了
```



渲染后效果：

我喜欢学习

我喜欢记笔记学习

我太喜欢学习了



### 换行用法的最佳实践：

> [!NOTE]
>
> 几乎每个 Markdown 应用程序都支持两个或多个空格进行换行，称为 `结尾空格（trailing whitespace)` 的方式，但这是有争议的，因为很难在编辑器中直接看到空格，并且很多人在每个句子后面都会有意或无意地添加两个空格。
>
> 由于这个原因，你可能要使用除结尾空格以外的其它方式来换行。幸运的是，几乎每个 Markdown 应用程序都支持另一种换行方式：HTML 的 `<br>` 标签。
>
> 为了兼容性，请在行尾添加“结尾空格”或 HTML 的 `<br>` 标签来实现换行。



## 四. 强调语法

通过将文本设置为粗体或斜体来强调其重要性。

## 粗体（Bold）

要加粗文本，请在单词或短语的前后各添加两个星号或下划线。如需加粗一个单词或短语的中间部分用以表示强调的话，请在要加粗部分的两侧各添加两个星号 。

> [!TIP]
>
> (考虑兼容性，多使用**星号**)

**Hello world**

**Hello** world



## 斜体（Italic）

要用斜体显示文本，请在单词或短语前后添加一个星号或下划线。要斜体突出单词的中间部分，请在字母前后各添加一个星号，中间不要带空格。

*Hello world*

> [!NOTE]
>
> 要同时用粗体和斜体突出显示文本，请在单词或短语的前后各添加三个星号或下划线。
>
> ```
> ***Hello world***
> ```
>
> 渲染后效果：
>
> ***Hello world***



## 删除线

若要删除单词，请在单词前后使用两个波浪号`~~`。

```text
~~世界是平坦的。~~ 我们现在知道世界是圆的。
```

渲染后效果：

~~世界是平坦的。~~ 我们现在知道世界是圆的。



## 下划线

`<u>`文本`</u> `  表示给选中文本加下划线

Typora中使用快捷键Ctrl + U 即可得到下划线标记

```
<u>给这段文字加下划线</u>
```

渲染后效果：

<u>给这段文字加下划线</u>



## 设置字体高亮

```
==这段文字会有高亮效果==
```



==这段文字会有高亮效果==





## 五. 引用语法

要创建块引用，请在段落前添加一个 `>` 符号

块引用可以包含多个段落。为段落之间的空白行添加一个 `>` 符号即可分段。

(Typora回车即可自动分段，生成的Markdown文件会自动添加 `>` 符号)

```
>fsafa
>adsasd
```

渲染后效果：

>fsafa
>adsasd



```
>Hello world
>
>hahaha
```

渲染后效果：

> Hello world
>
> hahaha

若想嵌套块引用，在要嵌套的段落前添加一个 `>>` 符号。

> [!WARNING]
>
> 由于Typora回车即可自动分段，嵌套快引用不必这么麻烦，生成的Markdown文件会自动添加 `>>` 符号，但为了兼容性还是避免使用嵌套快引用

```
>Hello world
>>Hello C++
```

渲染后效果：

> Hello world
>
> > Hello C++

## 六. 列表语法

## 有序列表

要创建有序列表，请在每个列表项前添加数字并紧跟一个英文句点以及一个空格。数字不必按数学顺序排列，但是列表通常应当以数字 1 起始，并自动排序。

1. First item

2. Second item

3. Third item

   1. Indented item
   2. Indented item
      - Hello Markdown

4. Fourth item

   > [!CAUTION]
   >
   > 使用Typora时，按回车列表会自动增加，因此使用嵌套列表时，需要手动调整使用快捷键“Ctrl+[”减少当前行缩进，“Ctrl+]”增加当前行缩进



## 无序列表

要创建无序列表，请在每个列表项前面添加破折号 (-)、星号 (*) 或加号 (+) 以及一个空格。缩进一个或多个列表项可创建嵌套列表。

- First item

- Second item

- Third item

  - Indented item

    > Hello world

  - Indented item

- Fourth item

  

  

## 在列表中嵌套其他元素

要在保留列表连续性的同时在列表中添加另一种元素，请将该元素缩进四个空格或一个制表符

(Typora似乎只需要换行，然后会在Markdown语句中会自动创建缩进)



## 任务列表

任务列表使您可以创建带有复选框的项目列表。在支持任务列表的Markdown应用程序中，复选框将显示在内容旁边。要创建任务列表，请在任务列表项之前添加破折号`-`和方括号`[ ]`，并在`[ ]`前面加上空格。要选择一个复选框，请在方括号`[x]`之间添加 x 。

```text
- [x] Write the press release
- [ ] Update the website
- [ ] Contact the media
```

渲染后效果：

- [x] Write the press release
- [ ] Update the website
- [ ] Contact the media



## 七. 代码语法

## 只表示单词或短语

要将单词或短语表示为代码，请将其包裹在反引号 ('') 中，这样就会将``中内容表示为代码，显示未渲染的原始内容。

```
e.g. 粗体强调的Markdown语法为`*hello world*`
```



渲染后效果：

e.g. 粗体强调的Markdown语法为`*hello world*`

如果你要表示为代码的单词或短语中已经包含一个或多个反引号，则可以通过将单词或短语包裹在双反引号(````)中。

```
e.g.  ``粗体强调的Markdown语法为`*hello world*` ``
```

渲染后效果：

e.g.  ``粗体强调的Markdown语法为`*hello world*` ``    (单反引号与双反引号接触时，要加一个空格区分开)

## 代码块

要创建代码块，在代码块之前和之后的行上使用三个反引号```

指定代码块语言：在开头的三个反引号后指定语言，可以在支持的Markdown渲染器中启用语法高亮。



Typora使用快捷键（ctrl + shift + K），即可创建一个空代码块

```c
#include<stdio.h>

int main(void){
    printf("Hello world");
    return 0;
}
```



## 八. 分割线语法

要创建分隔线，请在单独一行上使用三个或多个星号 (`***`)、破折号 (`---`) 或下划线 (`___`) ，并且不能包含其他内容。

以上三个分隔线的渲染效果看起来都一样:

```
***
---
___
```



渲染后效果：

***

---

___



## 九. 链接语法

### 表现为一个链接文本的链接

链接文本放在中括号内，链接地址放在后面的括号中，链接title可选。

> [!NOTE]
>
> 链接title是当鼠标悬停在链接上时会出现的文字，这个title是可选的，它放在圆括号中链接地址后面，跟链接地址之间以空格分隔。

超链接Markdown语法代码：`[超链接显示名](超链接地址 "超链接title")`

```text
这是一个链接 [Markdown语法](https://markdown.com.cn "最好的markdown教程")。
```

渲染效果如下：

这是一个链接 [Markdown语法](https://markdown.com.cn "最好的markdown教程")



### 只表现为链接地址，即网址或Email地址

使用尖括号可以很方便地把URL或者email地址变成可点击的链接。

```text
<https://markdown.com.cn>
```

渲染效果如下：

<https://markdown.com.cn>



## 十. 图片语法

添加图像，请使用感叹号 (`!`), 然后在方括号增加替代文本，图片链接放在圆括号里，括号里的链接后可以增加一个可选的图片标题文本。

插入图片Markdown语法代码：`![图片alt](图片链接 "图片title")`。

（同链接语法，只是前面加一个！即可）



## 链接图片

给图片增加链接，请将图像的Markdown 括在方括号中，然后将链接添加在圆括号中。



## 十一. 转义字符和脚注语法

要显示原本用于格式化 Markdown 文档的字符，请在字符前面添加反斜杠字符 \ 。

```text
\* Without the backslash, this would be a bullet in an unordered list.
```

渲染效果如下：

\* Without the backslash, this would be a bullet in an unordered list.

---

要创建脚注参考，请在方括号（`[^1]`）内添加插入符号和标识符。标识符可以是数字或单词，但不能包含空格或制表符。标识符仅将脚注参考与脚注本身相关联-在输出中，脚注按顺序编号。

在括号内使用另一个插入符号和数字添加脚注，并用冒号和文本（`[^1]: My footnote.`）。您不必在文档末尾添加脚注。您可以将它们放在除列表，块引号和表之类的其他元素之外的任何位置。



## 十二. 表格

Typora快捷键插入表格 ：Ctrl + T

在表格下方使用`:表格alt`



## 十三. 插入数学公式

行内输入：在两个`$`之间，输入Latex语法，行内插入数学公式

行间输入：在一对`$$	$$`包裹的区域，输入Latex语法，行间插入数学公式
